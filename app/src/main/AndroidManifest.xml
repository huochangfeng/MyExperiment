<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.huochangfeng.myexperiment" >

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:name=".application.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".epub.EpubTestActivity" />
        <activity android:name=".epub.ReadActivity" />
        <activity android:name=".epub.BatteryActivity" />
        <activity android:name=".breakpoint.TestActivity" />
        <activity android:name=".qrcode.BarCodeTestActivity" />
        <activity android:name=".netframe.NetFrameActivity" />
        <activity android:name=".rxjava.RxJavaTest" />
    </application>

</manifest>